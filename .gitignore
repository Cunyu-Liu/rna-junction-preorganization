# Large or mutable artifacts live outside the Git code root.
/data/
/weights/
/runs/
/outputs/
/tmp/
/cache/
/logs/*.log
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.venv/

# The authoritative contract is non-public and must stay on the protected
# remote project path; never publish the source document to public GitHub.
contract/1.1.docx
legacy/outputs/
legacy/contracts_v1.2.docx
source/

# macOS resource forks
._*

# R0 run logs/markers
/r1_run.log
/*.marker
